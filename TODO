kubestat.py
* CSV export
* Double-check comparison especially with multiple input files
* Rename keys/indices fields
* Add keys/indices fields
* Add context variable where present
* Summary to tree view
* Tree mode must respect 'with_changes'
* For filter - make "does NOT match regex"
* Toggle Megabytes | Mibibytes units
* Deleted Pod use case
* Make generic mechanism of parsing resources: CPU, memory, ephemeral-storage, storage
* Resource management: if requests/limits are not specified, set some flag when calculating totals (calculation is not precise)